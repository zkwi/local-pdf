# 真实文档审计修复

目标：用现有依赖解决七项已复现问题，保留原文件；完成回归、自审和生产构建后提交。

1. 页码：界面只验证语法，转换时按实际页数展开；范围无效或没有交集时明确报错。
2. 文档联网：Markdown 在进入活动 DOM 前清理不安全 HTML 和图片地址；排版 iframe 加禁止联网的 CSP，编码图片时也只读取本地资源。
3. 扫描长图：同时检查宽高，保留分片图片；超长图片分段，去掉保留区域中重复的 OCR 文字。报告说明图片区域不可编辑，扫描重排不再显示高把握。
4. 取消：图片解码、字体等待和图片读取支持取消，资源等待有上限，取消后释放 iframe 并让后续任务继续。
5. 兼容提示：EMF/WMF 等失败图片显示占位和格式提示；当前 PDF 字体不能输出的补充平面字符显示占位并计数，不新增图片解码或字体嵌入依赖。

验证：补上对应纯函数回归；真实浏览器检查联网、取消、页码和兼容提示；重跑上轮 40 份私有样本并检查关键产物。私有路径、文件名、内容及截图仅留在忽略目录。最后检查 diff、格式、完整测试和生产构建，仅提交本轮文件，不推送。
